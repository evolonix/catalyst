# Catalyst Demo

To run the Catalyst demo, first install the npm dependencies:

```bash
npm install
```

Next, run the development server:

```bash
npx nx dev catalyst
```

Finally, open [http://localhost:4200](http://localhost:4200) in your browser to view the website.
